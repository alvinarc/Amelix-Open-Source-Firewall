requests>=2.31.0
python-dotenv>=1.0.0
scikit-learn>=1.4.0
numpy>=1.26.0
pandas>=2.2.0
joblib>=1.3.0
tldextract>=5.1.0
opencv-python-headless>=4.9.0.0
qrcode[pil]>=7.4.2

# Note: the desktop GUI (amelix/gui.py) uses Tkinter, which ships with
# Python itself on Windows. On Linux it's sometimes a separate OS package
# (e.g. `sudo apt install python3-tk`) — see SETUP_GUIDE.md section 2.9.
